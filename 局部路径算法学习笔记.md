# 学习笔记

## 简单原理


### DWB：“采样-打分”制

### TEB：“图优化”制（橡皮筋）

### MPPI： “随机采样+迭代优化”制（是一种模型预测控制（MPC））

---

## 如何切换局部路径规划算法

注意，nav2_params_sim.yaml中的机器人2D碰撞形状只能是**footprint** 不能是robot_radius,因为代码优先读取footprint,而且全链路都用的是footprint。

出现的问题：

换完插件和参数之后rviz中点云正常出现但是发布目标点后无法按照路径移动（完全不动），后来让ai排查，发现是因为MPPI的参数类型不匹配。为什么无法直接肉眼看出呢？"**参数名对不上 → 被当成参数不存在 → 静默用了默认值**"。具体是嵌套 `motion_model（嵌套写法是Iron 版，Humble 不认））:` 被拍平成 扁平参数名，导致 Humble 要的扁平写法的参数不存在，于是静默回落成 `DiffDrive` + `0.5 m/s` 等默认值 → 车爬不动/不能横移 → 看起来"完全不动"。

---

##  核心原理对比

| 算法 | 工作原理 |
| :--- | :--- |
| **DWB** | 1. 在速度空间（线速度v、角速度ω）内采样多组控制量。<br>2. 对每组控制量模拟一段短时间内的运动轨迹。<br>3. 根据**安全性**、**贴路性**（贴近全局路径）、**速度**、**朝向**等多项指标打分。<br>4. 选择综合评分最高的轨迹执行。 |
| **TEB** | 1. 截取全局路径上的一段，转化为一系列离散的**位姿点**（Pose）。<br>2. 为每个位姿点分配**时间戳**，形成“弹性带”。<br>3. 在满足**避障**、**运动学约束**、**速度/加速度限制**等“外力”下，利用图优化（g2o）最小化轨迹的**总时间**或**平滑度**。<br>4. 输出优化后的一系列含时间的位姿，再转换为速度指令。 |
| **MPPI** | 1. 基于当前状态和上一帧的最优控制指令，加入**随机噪声**，生成N条（如1000-2000条）候选轨迹。<br>2. 根据预测模型，推演每条轨迹的未来状态（考虑障碍物、动力学等）。<br>3. 通过**成本函数**（含碰撞、偏差、平滑等项）计算每条轨迹的总成本。<br>4. 对所有候选轨迹按成本进行**加权平均**（概率加权），计算出当前最优的控制指令。<br>5. 下一帧重复此过程，持续迭代优化。 |

---

##  优缺点对比(ai总结)

| 特性 | **DWB** | **TEB** | **MPPI** |
| :--- | :--- | :--- | :--- |
| **轨迹平滑度** | 较平滑，可能有轻微顿挫 | **最平滑**，对机械硬件友好 | 平滑，随机采样可能导致轻微抖动 |
| **静态避障** | ✅ 优秀，评分机制清晰 | ✅ 优秀，但复杂障碍物可能失败 | ✅ 优秀 |
| **动态避障**（行人等） | ❌ 弱，无预测能力 | ⚠️ 一般，适合慢速变化环境 | ✅ **强**，通过多轨迹采样具备一定预测性 |
| **狭窄通道表现** | ⚠️ 一般，可能轻微摆动 | ✅ **最佳**，如门、走廊 | ⚠️ 一般 |
| **贴近全局路径** | ✅ 强，有明确成本项 | ✅ 强，但优化中可能被平滑性牺牲 | ⚠️ 中等，更注重避障 |
| **计算资源消耗** | **低** | 中等 | **高**（需要批量采样） |
| **实时性** | 高（可达100Hz以上） | 高（通常20-50Hz） | 中等（优化后可达100Hz以上） |

